sonar.projectKey=soham-alm_product-ui
sonar.projectName=Product-UI
sonar.projectVersion=1.0
sonar.host.url=https://sonarcloud.io
# Path to source directories (adjust as needed)
sonar.sources=src

# Exclude test and build artifacts
sonar.exclusions=**/node_modules/**,**/build/**,**/dist/**,**/*.test.js,**/*.test.jsx,**/*.spec.js,**/*.spec.jsx,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Include test files for coverage
#sonar.tests=src
#sonar.test.inclusions=**/*.test.js,**/*.test.jsx,**/*.spec.js,**/*.spec.jsx,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# Encoding
sonar.sourceEncoding=UTF-8

# Language-specific settings
#sonar.javascript.lcov.reportPaths=coverage/lcov.info
#sonar.typescript.tsconfigPath=tsconfig.json

# Coverage (ensure you run `npm test -- --coverage` or `yarn test --coverage` to generate lcov.info)
#sonar.javascript.coveragePlugin=lcov
