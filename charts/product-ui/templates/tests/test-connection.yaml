apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product-ui.fullname" . }}-test-connection"
  labels:
    {{- include "product-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
